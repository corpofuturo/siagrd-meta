[build]
  base = "apps/panel-web"
  command = "npm install -g pnpm && cd ../.. && pnpm install --frozen-lockfile && pnpm build:panel-web"
  publish = "apps/panel-web/.next"

[build.environment]
  NODE_VERSION = "20"

[[plugins]]
  package = "@netlify/plugin-nextjs"
