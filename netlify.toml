[build]
base = "apps/panel-web"
command = "cd ../.. && pnpm install --frozen-lockfile && pnpm --filter panel-web build"
publish = ".next"

[build.environment]
NODE_VERSION = "20"

[[plugins]]
package = "@netlify/plugin-nextjs"
